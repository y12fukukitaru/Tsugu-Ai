-- =============================================================
-- daily-reminder は、動いているのか。動いて「送らなかった」のか。
-- ---------------------------------------------------------------
--  2026-09-02、届いたメールは 6:00 の週次ブリーフ（agent-heartbeat）だけで、
--  8:00 のメールは届かなかった。daily-reminder の定期実行は
--  23:00 UTC ＝ 8:00 JST に組まれているので、次のどちらかになる。
--
--    ① 動いたが、送る相手・送る用事が無かった（正常）
--    ② 動かなかった／途中で落ちた（要対応）
--
--  この2つは、届かなかったという事実だけでは区別がつかない。
--  下の3つを流すと、どちらなのかが分かる。
--
-- 実行方法: Supabase Dashboard → SQL Editor に貼り付けて Run
--           読み取るだけなので、何度実行しても何も変わらない。
-- =============================================================


-- ---------------------------------------------------------------
-- 1. 直近の実行履歴（動いたかどうか）
-- ---------------------------------------------------------------
--  succeeded が並んでいれば、呼び出し自体は成功している。つまり①寄り。
--  failed があれば、その 結果 の欄に理由が出る。
--  そもそも行が出てこなければ、定期実行が止まっている（②）。
select j.jobname                                as ジョブ名,
       d.start_time at time zone 'Asia/Tokyo'   as 実行時刻_日本時間,
       d.status                                 as 結果,
       left(coalesce(d.return_message,''), 200) as 内容
  from cron.job_run_details d
  join cron.job j using (jobid)
 where j.jobname in ('daily-reminder', 'agent-heartbeat-daily')
 order by d.start_time desc
 limit 20;


-- ---------------------------------------------------------------
-- 2. 呼び出しに対する応答（関数が中で落ちていないか）
-- ---------------------------------------------------------------
--  1 が succeeded でも、それは「HTTPで叩けた」という意味でしかない。
--  関数の中で落ちていれば、ここに 500 が出る。
--    200 → 関数は正常に終わった（＝送る用事が無かっただけ。①）
--    401 → 呼び出しの鍵が合っていない
--    500 → 関数の中で落ちている（②）
--
--  【2026-09-02 追記】この2番は、この問いには使えないと分かった。
--  daily-reminder も agent-heartbeat も 0件だったのに、agent-heartbeat の
--  6:00 のメールは実際に届いている。net._http_response は短い期間で
--  掃除されるため、「行が無い＝送っていない」とは読めない。
--  0件は正常であって、異常の証拠にはならない。
--  関数の中で何が起きたかは、Dashboard → Edge Functions → Logs で見る。
select r.created at time zone 'Asia/Tokyo' as 時刻_日本時間,
       r.status_code                       as 応答,
       q.url                               as 宛先,
       left(coalesce(r.error_msg, r.content, ''), 300) as 中身
  from net._http_response r
  join net.http_request_queue q using (id)
 where q.url ilike '%daily-reminder%'
 order by r.id desc
 limit 10;


-- ---------------------------------------------------------------
-- 3. そもそも、どう組まれているか
-- ---------------------------------------------------------------
--  active が false なら、止められているということ。
--  schedule が期待どおり '0 23 * * *' かどうかもここで確かめる。
select jobid,
       jobname  as ジョブ名,
       schedule as 時刻,
       active   as 動作中,
       left(command, 300) as 中身
  from cron.job
 where jobname ilike '%reminder%'
    or command ilike '%daily-reminder%'
 order by jobid;


-- ---------------------------------------------------------------
-- 4. 直近30時間の実行だけを見る（結果が短いので、画面を繰らずに済む）
-- ---------------------------------------------------------------
--  1番は20行返るため、表を下までスクロールしないと今朝の行が見えない。
--  今朝のことだけ知りたいときは、こちらを使う。
select j.jobname                              as ジョブ名,
       d.start_time at time zone 'Asia/Tokyo' as 実行時刻_日本時間,
       d.status                               as 結果,
       d.return_message                       as 内容
  from cron.job_run_details d
  join cron.job j using (jobid)
 where d.start_time > now() - interval '30 hours'
 order by d.start_time desc;


-- =============================================================
-- 【2026-09-02 の実行結果】
-- ---------------------------------------------------------------
--  3 → ジョブは存在し、active = true、schedule = '0 23 * * *'（8:00 JST）。
--       止められてはいない。
--  1 → 直近20回すべて succeeded。内容はどれも "1 row"（net.http_post が
--       要求を積んだという意味）。daily-reminder も agent-heartbeat-daily も
--       毎日きちんと呼ばれている。
--  2 → 0件。ただし上に書いたとおり、これは異常の証拠にならない。
--
--  つまり「呼び出しまでは正常」。残る分からなさは関数の中だけで、
--  それはこのリポジトリの外にあるので、SQLではこれ以上詰められない。
--  次に見るのは Dashboard → Edge Functions → daily-reminder → Logs。


-- =============================================================
-- 結果の読み方
-- ---------------------------------------------------------------
--  この一式で分かるのは「呼ばれたかどうか」までで、「送ったかどうか」は
--  分からない。2番で分かるつもりでいたが、上の追記のとおり当てにならない。
--  境目をはっきりさせておく。
--
--  1（または4）が succeeded
--      → 定期実行から関数までは届いている。ここから先は関数の中の話で、
--        SQLでは見えない。Dashboard → Edge Functions → daily-reminder →
--        Logs を見る。
--
--  1 が failed
--      → 関数を呼ぶところで失敗している。内容 の欄に理由が出る。
--
--  1 に行が出ない、または 3 の 動作中 が false
--      → 定期実行が止まっている。9/12 のローンチまでに戻す必要がある。
--
--  いずれの場合も、daily-reminder の中身（Dashboard → Edge Functions →
--  daily-reminder → Code）を貼っていただければ、monthly-report と同じように
--  リポジトリに取り込んで、差出人名と件名を揃えます。いまはこの関数だけが
--  リポジトリの外にあり、こちらから中身を確かめられません。
--  取り込めば、次からは同じ問いをSQLで追い回さずに済む。
-- =============================================================
